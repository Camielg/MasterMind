package com.company;

public class HasPlayerWon {

    private boolean CheckIfPlayerHasWon;
    private String Correcttoken;
    private String WrongPositionToken;

    public HasPlayerWon(boolean checkIfPlayerHasWon, String correcttoken, String wrongPositionToken){
        this.CheckIfPlayerHasWon =CheckIfPlayerHasWon() ;
        this.Correcttoken= "X";
        this.WrongPositionToken= "O";
    }



    public boolean CheckIfPlayerHasWon(){
       /* if(board[0] = fisrtDigit && board[1]== secondDigit && board[2] == thirdDigit && board[3] == fourthDigit]){
        return true;
    } else {
        return false;
    }
        */
    }


    public boolean CheckCorrectDigitPlace(){
       /* if(CheckIfPlayerHasWon = false){
            if(firstDigit = board[0]){
                Correcttoken
            } else if(firstDigit = board[1]){
            WrongPositionToken
            } else if(firstGigit = board[2]{
            WrongPositionToken
            } else if(firstDigit = board[3]{
            WorngPositionToken
            }
        herhalen voor secondDigit, thirdDigit en fourthDigit.
        array aanmaken om token in kwijt te kunnen
        waarschijnlijk een bug waneer er 2 dezelfde getallen in de te raden code staan

        }

        */
    }



}
